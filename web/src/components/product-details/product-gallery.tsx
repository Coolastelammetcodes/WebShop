import { Box } from "@mui/material";

export default function ProductGallery() {
    return (
        <Box component="aside" sx={{display:"flex", flexDirection: "column", gap: "0.5rem"}}>
            
        </Box>
    );
}